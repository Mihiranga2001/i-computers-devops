services:

  frontend:

    image: gaurawawickramasinghe/icomputers-frontend:latest

    container_name: frontend

    ports:
      - "8080:80"

    depends_on:
      - backend



  backend:

    image: gaurawawickramasinghe/icomputers-backend:latest

    container_name: backend

    ports:
      - "3000:3000"

    env_file:
      - ../i-computers-backend/.env